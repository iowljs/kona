{
  "name": "kona-server",
  "version": "0.0.3",
  "description": "Kona is a simple backend server that scales",
  "main": "bin/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified, we are working on it.\" && exit 1",
    "start": "node bin/server.js",
    "start:dev": "NODE_ENV=development node bin/server.js",
    "start:staging": "NODE_ENV=staging node bin/server.js",
    "start:production": "NODE_ENV=production node bin/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.2",
    "express": "^4.16.3",
    "nodemailer": "^4.6.5",
    "request": "^2.87.0"
  }
}
