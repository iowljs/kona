{
  "name": "kona-server",
  "version": "0.0.1",
  "description": "Kona is a simple backend server that scales",
  "main": "bin/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.2",
    "express": "^4.16.3"
  }
}
